# Screen Flow

Same sequence as the system flow:

```text
Login → Home → Movie listing → Show selection → Seat selection → Customer details → Booking summary → Confirmation
```

After phase 08, confirmation offers a clean start for a new booking.