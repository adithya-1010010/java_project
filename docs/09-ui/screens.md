# Screens

Per-screen documentation is added as each screen is implemented (Phases 04–08). Template fields per screen: purpose, controls, inputs, validation, navigation destination, controller, service calls, data loaded.