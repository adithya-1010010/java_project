# Test Plan

Tests are defined phase-by-phase in `docs/12-phases/phase-NN.md` and the authoritative implementation plan (§11). Mapped requirements appear in `docs/02-requirements/requirements.md`.

Run:

```bash
mvn test
```